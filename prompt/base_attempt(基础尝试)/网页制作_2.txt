turn1:
请用 HTML 和 CSS 编写一个现代化的登录页面。
核心风格：使用 Glassmorphism（毛玻璃/磨砂玻璃）效果。
细节：
页面背景是一个色彩鲜艳的渐变色（比如紫色到蓝色的对角线渐变）。
登录框位于屏幕正中央，背景是半透明的白色，带有模糊效果（backdrop-filter: blur）。
包含“用户名”和“密码”两个输入框，以及一个圆角的渐变色登录按钮。
输入框的边框要非常细，并且去掉默认的轮廓线。

turn2:
请在此基础上为我增加注册页面