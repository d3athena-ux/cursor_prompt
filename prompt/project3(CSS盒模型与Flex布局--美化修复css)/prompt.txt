你是资深前端工程师。请在我现有页面中新增一个“商品展示区（Products）”，并提供一套可复用的商品卡片样式。
【商品区要求】
● 标题：本期推荐
● 商品卡至少 6 个，使用假数据（名称、价格、标签、简短描述）
● 每个卡片包含：图片（固定比例）、商品名、价格、标签 badge、按钮“加入购物车”（本节先不实现购物车逻辑，点击只弹出提示即可）
● 布局：桌面端一行 3 列，平板 2 列，手机 1 列（用 flex 或 grid）
● 卡片要求：等高、hover 上浮、阴影渐变、按钮有主次（主按钮更醒目）
【输出要求】
● 不要推翻原页面结构
● 给出需要新增/替换的 HTML 片段 + CSS 片段 + JS 片段（最小改动）
● 给出验收清单（至少 10 条，包含响应式检查）

商品标题过长会把卡片撑高，导致按钮不对齐。
请保持 HTML 结构不变，给我最小 CSS 修复方案，实现：
● 标题最多两行，超出省略号
● 卡片内部按钮区域始终贴底对齐
并说明关键 CSS 原理。