你是资深前端工程师。请在不改变“数据驱动渲染”架构的前提下，升级商品卡片 UI，让页面更像真实电商。
【商品卡必须包含】
● 图片：固定比例（例如 4:3），object-fit: cover
● 标题：最多显示两行，超出省略号
● 价格：主价格加粗；可选显示原价划线 + 折扣 badge
● 标签：用 badge 显示（例如“热卖”“新品”“包邮”）
● 按钮：主按钮“加入购物车”，次按钮“查看详情”（或点击卡片查看）
【列表布局】
● 手机 1 列，平板 2 列，桌面 3~4 列（合理断点）
● 卡片等高，不因标题长短导致按钮上下跳
【交互与边界】
● 筛选/搜索导致没有商品时，显示“无结果空状态”，并提供“重置筛选”按钮
● 保持事件委托结构不变
【输出要求】
● 只输出需要替换的 CSS + renderProducts 相关 JS（最小改动）
● 给验收清单（至少 18 条，包含空状态）

商品标题过长会把卡片撑高，导致按钮不对齐。
请保持 HTML 结构不变，给我最小 CSS 修复方案，实现：
● 标题最多两行，超出省略号
● 卡片内部按钮区域始终贴底对齐
并说明关键 CSS 原理。