你是资深前端工程师。请在不改变页面结构与现有功能的前提下，为页面增加“克制、现代、性能友好”的动效体系。

【动效要求】

卡片 hover：上浮（translateY）+ 阴影增强，transition 200~300ms

主按钮：轻微发光或渐变流动（不要过强）

区块进入动画：当 section 进入视口时淡入 + 上移（一次性触发即可）

【可访问性/性能要求】

如果用户系统设置 prefers-reduced-motion: reduce，则禁用或显著减弱动画

不要引入任何外部库

【输出要求】

给出需要新增/替换的 CSS

给出需要新增的 JS（如果使用 IntersectionObserver）

给出验收清单（至少 12 条，包含“减少动态效果”验证）

当前动画有点“花”，请保持实现方式不变，只把动效调得更克制：

缩短统一时长

降低位移幅度

降低发光强度

只输出需要修改的 CSS 参数，并说明理由。