你是资深前端工程师。请在我现有电商首页基础上，实现“搜索 + 分类筛选 + 排序”的完整闭环，要求代码可维护、可扩展。
【状态管理要求（强制）】
● 新增 state 对象：{ keyword: "", category: "all", priceRange: "all", sort: "default" }
● 新增一个统一入口函数：updateProducts()，内部负责：
1）从 products 计算得到 filtered
2）应用排序
3）调用 renderProducts(filtered)
4）更新结果统计（显示 N 件）

【交互要求】
● 搜索框：输入时实时搜索（或回车触发也行，但需说明），支持清空按钮
● 分类：点击分类按钮筛选，并有当前分类高亮
● 提供“重置筛选”按钮：一键恢复 state 并刷新列表
● 所有筛选组合可叠加（例如：分类=鞋子 + 关键字=跑步 + 排序=价格升序）

【输出要求】
● 不要重写全部项目，只给最小改动方案：需要新增/替换的 HTML 片段（如按钮/统计区域）、JS 片段、必要 CSS
● 给出验收清单（至少 20 条），其中必须包含 5 条“组合筛选用例”